version: '3.1'

services:
  webapp:
    image: "{{ image_name }}"
    deploy:
      replicas: 1
    ports:
      - "3000:3000"
    networks:
      - application

networks:
  application:
    external: true